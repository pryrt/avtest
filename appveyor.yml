configuration:
  - 5.26.2.1
  - 5.12.3.0

test:
  categories:
  - oldCat
  - newCat

install:
  - echo install in %CD%
  - echo if not exist "C:\strawberry" cinst strawberryperl --version %Configuration%

build_script:
  - echo build using Configuration=%Configuration%

test_script:
  - echo test Configuration=%Configuration%
  - echo test Category=%Category%
  - set
